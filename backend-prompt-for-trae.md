# 🚀 Trae Solo / Cursor 后端开发提示词 (Prompt)

请将以下提示词（Prompt）复制并粘贴给 Trae Solo / Cursor / Claude 等 AI 编程助手，用于一键生成本项目的高质量后端代码架构。

---

## 🎯 角色设定
你是一个具有复合背景的**资深顶级量化系统架构师**与**Python/FastAPI高级开发专家**。你精通 A股市场的机制（包括其特有的“玄学”特征）、熟悉 `WorldQuant Alpha 101` 因子库的计算原理，擅长使用变分贝叶斯隐马尔可夫模型(HMM)进行时间序列预测，并具有将大语言模型（特别是 DeepSeek）深度融合到数据流处理中的丰富实战经验。

## 📁 项目背景与定位
我们要开发一款名为**“A股量化分析系统”**的Web运用后端服务。
核心理念是：“让散户拥有机构级的量化分析能力”。
核心底线：所有输出均为**概率判断与客观分析**，严禁输出明确的“买入/卖出指引”，需构建合规过滤防火墙。

## 🛠 技术栈要求
*   **核心框架**: `FastAPI` (Python 3.10+) 异步架构
*   **数据生态**: `AkShare` (主力开源数据源), `pandas`, `numpy`, `TA-Lib`
*   **贝叶斯引擎**: `PyMC` 或 `hmmlearn` (处理市场多状态HMM模型)
*   **大模型接入**: `DeepSeek API` (使用 `openai` 官方库兼容接入)
*   **数据库缓存**: `PostgreSQL` (持久化) + `Redis` (高速缓存与WebSocket发布订阅)
*   **实时通信**: `FastAPI WebSockets`
*   **任务调度**: `APScheduler` 或 `Celery` (执行定时因子计算)

## 🏗 核心模块开发任务说明

请逐步为我构建以下核心后台模块，要求代码结构清晰、高内聚低耦合，符合领域驱动设计（DDD）思想：

### 1. 数据采集网关 (`/services/data_fetcher.py`)
*   利用 AkShare 异步获取：
    *   实时盘口数据、A股核心宽基指数（上证、深成、创业板）行情
    *   板块资金流向、北向资金（陆股通）实时数据
    *   个股历史K线（前复权，日线/分钟线）
    *   东方财富/新浪财经的 实时滚动新闻/公告
*   **要求**: 设计容错重试机制与限频锁，避免触发数据源IP封禁，实时数据存入 Redis，历史数据入 PG。

### 2. Alpha量化因子引擎 (`/services/factor_engine.py`)
*   基于《101 Formulaic Alphas》以及A股特色，实现基础的因子的批量计算：
    *   技术面因子（动量、均值回归、波动率、MACD/KDJ变种）
    *   基本面因子代理计算（如ROE估算、动态PE/PB）
*   要求使用 `pandas` 向量化加速，避免 `for` 循环，暴露出 `calculate_factors(df: pd.DataFrame)` 统一接口。

### 3. A股玄学（特色）因子计算器 (`/services/metaphysics_engine.py`)
*   实现以下结构化输出的打分器：
    *   **日历效应**: 判断当前是否属“春季躁动”、“两会行情”、“年底结算”等时间窗口。
    *   **北向与主力资金偏好**: 将资金净流入转化为 0-100 的情绪热度得分。
    *   **政策驱动 NLP**: 将最近24小时重大新闻输入 DeepSeek Flash 模型，提取“利好/中性/利空”情绪值，转化为情感因子。

### 4. 贝叶斯概率预测引擎 (`/services/bayesian_hmm.py`)
*   **市场级状态机**: 训练一个隐马尔可夫模型（HMM），将市场状态抽象为4类：`强势牛市`、`单边熊市`、`宽幅震荡`、`结构性分化`。
*   根据不同市场状态，动态分配“五维画像”（技术面、基本面、资金面、消息面、情绪面）的权重。
*   **输出格式**: 必须是概率分布，例如 `{"看涨概率": 0.68, "震荡概率": 0.22, "看跌概率": 0.10}`。

### 5. DeepSeek 智能分析中枢 (`/services/ai_analyzer.py`)
*   **三层分析架构设计**:
    *   **L0层 (规则引擎)**: 过滤80%常规异动（无需调用LLM）。
    *   **L1层 (Flash模型)**: 处理日常新闻摘要与情感判断（低 Token 消耗）。
    *   **L2层 (Pro模型深度研判)**: 处理个股异动综合研判。将 L0/L1 产生的数据、因子以及五维雷达图数据，压缩注入给 DeepSeek-Pro。
*   **提示词强约束**: 在调用 LLM 时的 System Prompt 中写死：“你是一个量化分析工具的拟人化输出模块。你只能给出数据事实、概率趋势与逻辑推导。当你分析到看好时，只能用类似‘数据呈现XX向上的概率特征’，**绝对禁止**使用‘建议买入’、‘抄底’等投资建议词汇。”

### 6. 合规与输出防火墙 (`/middleware/compliance.py`)
*   在 FastAPI 的响应中间件中，增加关键词过滤拦截机制。一旦返回 JSON的 `ai_analysis` 字段中出现敏感违禁词汇，强制正则替换或附加免责声明后缀：“【免责声明】AI分析基于历史模型和概率测算，不代表未来表现，仅供个人学习交流使用，不构成任何投资建议。”

### 7. WebSocket 推送管道 (`/api/websockets.py`)
*   建立前端订阅通道，支持根据股票代码（订阅具体股票）或 "market"（订阅大盘）进行实时 JSON PUSH，推送突变指针（如某股概率突变>30%）。

---
## 🏃‍♂️ 首期开发步骤提示：
为了保持节奏，请不要一次性输出所有代码。我们分步骤来：
**第一步：** 请帮我初始化基于 FastAPI 的项目目录树结构，涵盖上述的目录划分。并给出 `requirements.txt` 和核心 `main.py` 的骨架代码。
**第二步：**（等待我确认后），我们再重点编写 `data_fetcher.py` 和 `factor_engine.py` 的实现。

请问你听懂了吗？如果听懂了，请直接执行第一步的代码输出。
